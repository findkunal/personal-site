---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostCard from '../../components/PostCard.astro';

const posts = (
  await getCollection('blog', ({ data }) => data.lang === 'en' && !data.draft)
).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const latest = posts.slice(0, 5);
---
<BaseLayout
  lang="en"
  pageType="home"
  title="Kunal &mdash; Notes on software, systems, and culture"
  description="Personal blog of Kunal. Essays in English and Gujarati."
>
  <h1>Hi, I&rsquo;m Kunal.</h1>
  <p>
    I write about software, systems, and the things I think about in between.
  </p>
  <p>
    My working philosophy is closer to Stoicism than anything else: separate what
    you control from what you don&rsquo;t, then let the rest go. Emotions are
    signals, not commands. Discipline is just self-respect made visible. The
    point isn&rsquo;t to be unshakeable. It&rsquo;s to be a little less reactive
    each year, and a little more honest about your own role in your own life.
  </p>
  <p>
    I keep coming back to the same questions: how to think clearly, how to do
    good work without hollowing yourself out, and how to stay kind without
    being soft.
  </p>
  <p>
    Most posts here are in English; some are in
    <a href="/gu/">ગુજરાતી</a>.
  </p>

  <h2>Latest posts</h2>
  {latest.length === 0 ? (
    <p><em>No posts yet &mdash; check back soon.</em></p>
  ) : (
    <ul class="post-list">
      {latest.map((post) => (
        <PostCard
          href={`/en/blog/${post.slug.replace(/^en\//, '')}/`}
          title={post.data.title}
          description={post.data.description}
          pubDate={post.data.pubDate}
          lang="en"
        />
      ))}
    </ul>
  )}

  {posts.length > 5 && (
    <p><a href="/en/blog/">See all posts &rarr;</a></p>
  )}
</BaseLayout>
